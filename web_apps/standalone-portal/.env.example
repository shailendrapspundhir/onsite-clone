NEXT_PUBLIC_AUTH_GRAPHQL_URL=http://localhost:3001/graphql
NEXT_PUBLIC_USER_GRAPHQL_URL=http://localhost:3002/graphql
NEXT_PUBLIC_JOB_GRAPHQL_URL=http://localhost:3003/graphql
NEXT_PUBLIC_LOG_LEVEL=INFO
# Set NEXT_PUBLIC_LOG_LEVEL=DEBUG for verbose logs (debugs profile update errors, API failures, publish/apply/withdraw, etc.)
# Falls back to LOG_LEVEL for server-side. Copy to .env and customize if needed. Standalone app uses direct fetch to these APIs.
